# -*- coding: utf-8 -*-
import random

# Описание :
# Функция генерирует массив случайных чисел заданного размера и диапазона.
# Аргументы :
# - size (int) : Размер массива.
# - min (int) : Минимальное значение элементов.
# - max (int) : Максимальное значение элементов.
# Возвращает :
# - list : Список случайных чисел.
def generate_random_array(size: int, min: int, max: int) -> list:
    return [random.randint(min, max) for _ in range(size)]
